#pragma once

#include <string>
#include <sstream>
#include <bitset>

std::string hexadecimalToBinary(std::string);
std::string hexadecimalToOctal(std::string);
std::string hexadecimalToDecimal(std::string);

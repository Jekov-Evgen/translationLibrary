#pragma once

#include <string>
#include <sstream>
#include <bitset>
#include <cstring>

const char* hexadecimalToBinary(const char*);
const char* hexadecimalToOctal(const char*);
const char* hexadecimalToDecimal(const char*);
